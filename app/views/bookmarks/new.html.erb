<h1>Add a movie in <%= @list.name %> category </h1>

<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>

<%= link_to "Back", list_path(@list) %>
